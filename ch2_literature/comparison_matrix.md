| Citation key | Privilege assumptions | Collection pattern | Telemetry signals | Application code modification | Evaluation performed | Overhead reported | Explicit constraints discussed | Fit or break under constrained CI/CD |
|-------------|----------------------|--------------------|------------------|-------------------------------|---------------------|-------------------|-------------------------------|-------------------------------------|
| correiaMonintainerOrchestrationindependentExtensible2023 | Host-based access required to deploy node monitors on each physical/virtual node. Access to cgroups for container-level metrics. RAPL requires privileged access for energy consumption measurements at CPU level. | Hybrid push model. Container agents push data to node monitors. Node monitors aggregate data from core services (pull) and container agents (push), then push aggregated data to integration layer (REST API). Prometheus format exposed. | Metrics: infrastructure, container, and application-level metrics including CPU, memory, disk, GPU, energy, connections, requests, response times. | Yes. Container agents must be deployed inside containers. One agent per application required. | Comparison with Docker-stats, cAdvisor, Prometheus, ConMon, Sysdig, Instana, Datadog, Dynatrace, Sensu. Scalability and extensibility analysis. | Not explicitly quantified. States negligible overhead of cAdvisor/Prometheus/Grafana. | Requires node-level access; container agents increase deployment complexity; RAPL limited to Intel CPUs; external hardware cannot isolate container energy. | Breaks |
| dingaEmpiricalEvaluationEnergy2023 | Host-based access required. Monitoring tools deployed on host via cgroups. Zipkin requires application-level integration. | Host-based collection via Metricbeat, Netdata, cAdvisor; Zipkin traces pushed from application. | Metrics: CPU, memory, network, execution time. Traces: Zipkin distributed traces. Energy: machine-level. | No for metrics; Yes for Zipkin (Java Sleuth). | TrainTicket benchmark, 41 microservices, factorial design, 390 runs, statistical testing. | Energy +1.5% to +12.9%; Zipkin highest CPU and RAM usage. | Limited to Docker; requires physical power meter; cannot isolate container energy. | Breaks |
| grossmannMonitoringContainerServices2017 | Privileged container required to access Docker socket. | Push-based host monitoring via Monit → Caddy → PyMon. | Metrics: CPU, memory, container state, host metrics. | No. External Docker inspection only. | Kubernetes vs Docker Swarm comparison on Raspberry Pi cluster. | No explicit numbers; Kubernetes higher idle CPU/memory than Swarm. | Requires privileged container; storage limited; Kubernetes overhead high for edge. | Breaks |
| hasanthEvaluatingPerformanceSidecarbased2024 | Kernel eBPF access required for Cilium; sidecars for Istio/Linkerd. | eBPF host-based (Cilium) vs sidecar proxies (Istio/Linkerd). | Metrics: latency, throughput, error rates; tracing and traffic telemetry. | No. | Kubernetes cluster on bare metal; Online Boutique app; Locust load tests. | Linkerd fastest; Istio slowest; Envoy sidecars ~5MB per instance. | Kernel eBPF dependency; sidecar resource cost; missing features in Cilium. | Breaks |
| InvestigatingPerformanceOverhead | Not stated | Export-based tracing via OpenTelemetry Collector. | Traces; throughput and latency metrics. | Yes. Manual instrumentation for serverless; auto-instrumentation for microservices. | TechEmpower and SeBS benchmarks; profiling analysis. | Throughput −19–80%; latency +7–175%. | Serverless cold-start sensitivity; configuration overhead. | Breaks |
| janecekContainerWorkloadCharacterization2021 | Host kernel tracing (LTTng) required. | Host-based kernel tracing filtered by namespace. | Kernel-level metrics and execution traces. | No. | Ubuntu + Docker workloads; clustering analysis. | Minimal overhead reported qualitatively. | Requires kernel tracing; post-processing needed. | Breaks |
| kannanDesigningLightweightNetwork2024 | CAP_BPF, CAP_NET_ADMIN, CAP_PERFMON required. | Kernel eBPF with userspace aggregation and exporter. | Flow-level metrics (5-tuple, packets, bytes). | No. | Bare-metal and OpenShift benchmarks. | ~10% throughput drop; ~0.5% CPU per node in production. | Hash map limits; enrichment latency; kernel dependency. | Fits |
| karkanPerformanceOverheadOpenTelemetry | Cluster-admin and node-level privileges required. | Multi-tier OpenTelemetry collector pipeline. | Distributed traces. | No. | Kubernetes clusters; sampling strategies evaluated. | Tail-based sampling +71% CPU vs head-based. | Stateful collectors; tuning complexity; network amplification. | Partially fits |
| levinViperProbeUsingEBPF2020 | Host root / kernel eBPF required. | Host-based eBPF probes pushing to Kafka. | Kernel-level metrics per container. | No. | EC2 cluster with Hipster Shop workload. | CPU 10–15%; latency up to 60%. | eBPF invocation overhead; kernel limitations. | Breaks |
| liuJCallGraphTracingMicroservices2019 | None stated. | In-container agents with push to transfer cluster. | Traces. | No. | Large-scale JD.com production system. | CPU ~1–5%; negligible latency impact. | Ring buffer overflow; bandwidth growth. | Partially fits |
| norgrenOPTIMIZINGDISTRIBUTEDTRACING | None stated. | Daemonset vs sidecar OpenTelemetry collectors. | Traces. | No. | K3s clusters; emulated financial workload. | CPU +46.5%; memory +47.5%. | Cloud limits; TraceContext overhead. | Breaks |
| pappulaBuildingObservabilityFullStack2021 | Not stated | Not stated | Metrics, logs, traces | Yes | E-commerce app with chaos testing. | MTTD/MTTR reductions reported. | High telemetry volume; retention cost. | Not stated |
| piProfilingDistributedSystems2018 | Host kernel tracing required. | Host-based LTTng with Kafka/OpenTSDB. | Kernel metrics and logs. | No. | 9-node Hadoop/Spark cluster. | Minimal overhead (qualitative). | Kernel dependency; regex extraction complexity. | Breaks |
| raithEndtoEndFrameworkBenchmarking2022 | Not stated | Host-based DaemonSet push to InfluxDB. | Metrics, PSI, timestamps. | Not stated | Edge/cloud benchmarking. | <10% CPU; low memory. | Limited scale; custom configuration. | Not stated |
| ramachandranFUSEFaultDiagnosis2023 | Host-based or privileged container access required. | Host-based eBPF probes. | System call and disk metrics. | No. | AWS EC2 microservices tests. | Latency +13× to +45×. | Linux dependency; storage growth. | Breaks |
| sahuSidecarsCentralLane2023 | Not stated | Not stated | Microarchitectural metrics | Not stated | Envoy policy experiments. | Latency ~1.03–1.05×. | Cache effects; vCPU placement. | Not stated |
| salcedo-navarroK8sidecarModularKubernetes2025 | None | Chain-of-sidecars via operator. | Not stated | No | Kubernetes sidecar scaling tests. | JVM sidecars higher overhead; Go close to Envoy. | Inter-container HTTP overhead. | Fits |
| sandbergEvaluatingOpenTelemetrysImpact | Not stated | In-container OTel agent push + Prometheus pull. | Traces, metrics. | Yes | Spring microservices benchmarks. | Auto tracing +42% CPU; batching reduces to ~3.6%. | Java-only; memory storage. | Partially fits |
| sharmaEBPFEnhancedCompleteObservability2024 | Kernel eBPF required (Linux ≥5.15). | Host-based eBPF DaemonSet. | Metrics, traces, logs. | Optional | Bare-metal Kubernetes tests. | 21–214× lower CPU vs alternatives. | Kernel dependency; dual-layer observability. | Fits |
| sharmaIMPROVINGMICROSERVICESOBSERVABILITY2023 | Kernel eBPF required. | Host-based eBPF DaemonSet. | Metrics, traces, logs. | Optional | Bare-metal Kubernetes tests. | eBPF minimal overhead; cAdvisor highest. | Kernel dependency; security implications. | Fits |
| soldaniEBPFNewApproach2023 | Kernel eBPF required. | Host-based DaemonSet. | Metrics, logs, traces. | No | Experimental platform validation. | Not stated | Kernel coupling; verifier complexity. | Fits |
| tanResearchLightweightService2023 | Kernel eBPF required. | Host-based kernel implementation. | Network metrics and traces. | No | Bare-metal service mesh comparison. | Not stated | System-wide hook impact. | Fits |
| taylorOpenFormatScalable2020 | Not stated | Host-based transport-layer probe. | Metrics, logs, traces. | No | Enterprise benchmarks. | Minimal (qualitative). | Limited network semantics. | Unclear |
| TraceWeaverDistributedRequest | Host-based / privileged access required. | External eBPF or sidecar-based collection. | Traces and call graphs. | No | DeathStarBench + Alibaba data. | Not stated | Traffic interception required. | Breaks |
| usmanDESKDistributedObservability2023 | Not stated | Host-based DaemonSet with Kafka. | Metrics, logs, traces. | Yes | Kubernetes IoT workload tests. | ~2.5% resource usage. | Interval tuning critical. | Breaks |
